name: nbatch
display_name: nbatch
# This package provides batch processing utilities, not napari widgets
# It is registered with napari for visibility in the plugin ecosystem
visibility: public
categories:
  - Utilities
contributions:
  # No commands or widgets - this is a utility library
  commands: []
